import React from "react";
import "./Options.css";

const Options = () => {
  return (
    <div class="options-container">
      <div class='options'>
        <button class='job-preview'>Job Preview</button>
        <button>Applicants</button>
        <button>Match</button>
        <button>Messages</button>
      </div>
    </div>
  );
};

export default Options;
